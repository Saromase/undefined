{% trans_default_domain 'FOSUserBundle' %}

{% block body %}
    <div class="login-box">
        <div class="login-logo">
            <a href="{{ path('homepage') }}"><b>Isolonice</b>Services</a>
        </div>
        <!-- /.login-logo -->
        <div class="login-box-body">
            <p class="login-box-msg">
                Vous vous trouvez actuellement sur Jarvis, l'inteface Isolonice.
            </p>

            {{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'class': 'fos_user_registration_register'}}) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
            <div class="row">
                <div class="col-xs-6 pull-right">
                    <button type="submit" id="_submit" name="_submit" class="btn btn-primary btn-block btn-flat">{{ 'registration.submit'|trans }}</button>
                </div>
            </div>

            <a href="{{ path("fos_user_security_login") }}" class="text-center">J'ai déja un compte</a>
        </div>
        <!-- /.login-box-body -->
    </div>
{% endblock %}

